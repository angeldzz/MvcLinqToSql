@model Enfermo

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8">
            <h1 class="mb-4">Detalles del Enfermo: <strong>@Model.Apellido</strong></h1>
            <hr />  
            
            <table class="table table-borderless table-hover">
                <tbody>
                    <tr>
                        <td class="fw-bold text-secondary" style="width: 30%;">Inscripción:</td>
                        <td>@Model.Inscripcion</td>
                    </tr>
                    <tr>
                        <td class="fw-bold text-secondary">Dirección:</td>
                        <td>@Model.Direccion</td>
                    </tr>
                    <tr>
                        <td class="fw-bold text-secondary">Fecha de Nacimiento:</td>
                        <td>@Model.FechaNacimiento.ToString("dd/MM/yyyy")</td>
                    </tr>
                    <tr>
                        <td class="fw-bold text-secondary">Sexo:</td>
                        <td>@Model.sexo</td>
                    </tr>
                    <tr>
